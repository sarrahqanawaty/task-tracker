# Byte-compiled / optimized files
__pycache__/
*.py[cod]
*$py.class

# Virtual environments
venv/
.venv/
env/
ENV/

# Environment variables
# `.env` and `.env.local` only covered two spellings — `.env.production` and
# `.env.staging` were not ignored. Match every .env* and re-include the
# committed template explicitly.
.env*
!.env.example

# Testing and coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.mypy_cache/
.ruff_cache/

# Databases (local SQLite file, added later)
*.db
*.sqlite
*.sqlite3

# Packaging / build
build/
dist/
*.egg-info/

# Editors and OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db